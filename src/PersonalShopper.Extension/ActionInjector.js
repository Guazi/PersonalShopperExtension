var PERSONALSHOPPER = PERSONALSHOPPER || {};
PERSONALSHOPPER.INTERACTION = PERSONALSHOPPER.INTERACTION || {};


PERSONALSHOPPER.INTERACTION.actionInjector = (function () {
    var _targetFinder, _actionDecorator,
    Constr = function (targetFinder, actionDecorator) {
        _targetFinder = node;
        _actionDecorator = actionDecorator;
    },
    findNode = function (node) {
    }
} ());

var a = new PERSONALSHOPPER.INTERACTION.actionInjector();

